console.log("MCP Cursor Server Setup"); console.log("======================"); console.log("Node.js version:", process.version); console.log("Platform:", process.platform); const fs = require("fs"); const path = require("path"); if (fs.existsSync(path.join(__dirname, "src"))) { console.log(" src directory found"); const files = fs.readdirSync(path.join(__dirname, "src")); console.log("  - Files:", files.join(", ")); } else { console.error(" src directory not found"); } console.log("\nSetup complete!"); console.log("To start the server, run: npm start");
